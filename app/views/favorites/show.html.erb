<h1>Favorites</h1>
<p>
  tags:
<% @user.favorite_tags.each do |tag| %>
  <a href="<%= tag_url(tag) %>"><%= tag.name %></a>
<% end %>
</p>

<% @user.favorite_tag_questions.each do|question| %>
<p>
  <strong><%= question.title %></strong>
  <p>
    <%= question.body %>
  </p>
</p>
<% end %>
